using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuizToObject : MonoBehaviour
{

    public bool isQuizCompleted; 

    // Start is called before the first frame update
    void Start()
    {
        isQuizCompleted = false;
    }

}
